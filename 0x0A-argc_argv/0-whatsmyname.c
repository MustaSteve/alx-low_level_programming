#include <stdio.h>
#include "main.h"

/**
 * main - prints
 * @argc: int
 * @argv: array
 *
 * Return: Always 0 (Success)
 */
int main(int argc, char *argv[])
{
	printf("%s\n", argv[argc - argc]);
	return (0);
}
